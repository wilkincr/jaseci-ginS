# Jaclang equivalent code

with entry {
    n = 5;

    if n != 0:
        result = n / 0
        std.out("True");  # Use std.out for printing in Jaclang
    } else {
        std.out("False");
    }
}